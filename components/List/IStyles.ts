export interface IIconFavorite {
  favorite?: boolean;
}

export interface IContainer {
  position: string;
  paddingTop: string;
}
export interface IContainerCards {
  width: number;
  height: number;
  margin: string;
  overflow: string;
  flex: string;
}

export interface ICard {
  transtionSpan: number;
  transtionImage: number;
  opacity: number;
  borderRadius: string;
  background: string;
}
